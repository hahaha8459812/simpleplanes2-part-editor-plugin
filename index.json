{
  "version": "0.3.4",
  "releaseNotes": "Apply now notifies SP2 runtime refresh hooks, recalculates loaded mass, and marks designer structure changes. Runtime cache fields are hidden by default with a settings toggle. Update notices also appear in the panel title. Release packages now include BepInEx 5 Mono x64 so users do not need a separate BepInEx download."
}
